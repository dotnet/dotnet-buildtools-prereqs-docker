trigger:
  branches:
    include:
    - master
  paths:
    include:
    - src/centos7/*
pr: none

variables:
- template: ../variables/common-official.yml

jobs:
  - template: jobs/build-image-legs.yml
    parameters:
      imageBuilderPath: src/centos7/*
