trigger:
  branches:
    include:
    - master
  paths:
    include:
    - src/opensuse/*
pr:
  branches:
    include:
    - master
  paths:
    include:
    - src/opensuse/*

variables:
- template: variables/common.yml

jobs:
  - template: jobs/build-image-legs.yml
    parameters:
      imageBuilderPath: src/opensuse/*
