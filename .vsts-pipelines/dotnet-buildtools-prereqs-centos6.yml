trigger:
  branches:
    include:
    - master
  paths:
    include:
    - src/centos6/*
pr:
  branches:
    include:
    - master
  paths:
    include:
    - src/centos6/*

variables:
- template: variables/common.yml

jobs:
  - template: jobs/build-image-legs.yml
    parameters:
      imageBuilderPath: src/centos6/*
