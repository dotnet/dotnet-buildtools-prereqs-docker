trigger:
  branches:
    include:
    - master
  paths:
    include:
    - src/ubuntu14/*
pr:
  branches:
    include:
    - master
  paths:
    include:
    - src/ubuntu14/*

variables:
- template: variables/common.yml

jobs:
  - template: jobs/build-image-legs.yml
    parameters:
      imageBuilderPath: src/ubuntu14/*
