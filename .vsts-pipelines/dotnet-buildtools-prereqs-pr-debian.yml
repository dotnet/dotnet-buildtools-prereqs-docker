trigger: none
pr:
  branches:
    include:
    - master
  paths:
    include:
    - src/debian/*

jobs:
  - template: jobs/build-image-legs.yml
    parameters:
      isPullRequestBuild: true
      imageBuilderPath: src/debian/*
